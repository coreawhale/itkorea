const add = (x, y) => x + y; // (x ,y) => {return x + y} 와 같음;
const substract = (x, y) => x - y; // (x ,y) => {return x - y} 와 같음;
const square = (x, y) => x ** y; // (x ,y) => {return x ** y} 와 같음;
